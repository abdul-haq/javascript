let person={
    name:'Andrew',
    age:27,
    loc:'Philadelphia'
}
console.log(`${person.name} is ${person.age} and lives in ${person.loc}`)
person.age=person.age+1
console.log(`${person.name} is ${person.age} and lives in ${person.loc}`)